# nginx/nginx.conf
events {}

http {
    server {
        listen 80;

        location /api/ {
            proxy_pass http://apigateway:8080/;
        }

        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri /index.html;
        }
    }
}
